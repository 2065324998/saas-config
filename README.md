# saas-config

Multi-tenant SaaS configuration resolver. Resolves tenant configurations
by combining subscription tiers, compliance profiles, feature overrides,
and usage data to produce effective resource limits, feature entitlements,
and monthly invoices.
